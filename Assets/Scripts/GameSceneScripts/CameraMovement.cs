using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraMovement : MonoBehaviour
{
    //switching between looking up (only when melody is played by statue), looking down (repeating pattern)
    //play animation LookDownCamera and LookUpCamera

    //if all levels completed, then start walking into cave (Animation)
    //play animation WalkingIntoCave
}
